import UIKit

final class {{ module_info.name }}ViewController: ViewController, BindableType {
	var viewModel: {{ module_info.name }}ViewModel!
    fileprivate let scene = {{ module_info.name }}View()
    
    override func loadView() {
        view = scene
    }
    
    override func viewDidLoad() {
        super.viewDidLoad()
    }
    
    func bindViewModel() {
        
    }
}